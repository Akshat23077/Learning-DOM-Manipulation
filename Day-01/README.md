# Day 01 - Keyboard Input Display ⌨️

## 📌 What I built

A simple project that displays the key pressed by the user in real-time on the screen.

## 🧠 What I learned

* DOM selection using `querySelector`
* Handling keyboard events using `keydown`
* Using `event.key`
* Updating UI dynamically with `textContent`

## ⚡ Challenges faced

* Handling space key separately

## 🚀 Future Improvements

* Add animations
* Show key codes
